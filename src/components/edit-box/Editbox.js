import React from 'react';
import { Button } from 'antd-mobile';
import styles from './Editbox.css';

const Editbox = () => {
  return (
    <div className={styles.h5_page}>
      <Button type="primary" size="small" inline>small</Button>
    </div>
  );
};

export default Editbox;
